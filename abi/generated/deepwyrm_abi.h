/* @generated by abi-gen from abi/schema.
 * Do not edit this file directly. */

#ifndef DEEPWYRM_ABI_GENERATED_H
#define DEEPWYRM_ABI_GENERATED_H

#include <stddef.h>
#include <stdint.h>

#if defined(__cplusplus)
#define DW_STATIC_ASSERT static_assert
#define DW_ALIGNOF alignof
#else
#define DW_STATIC_ASSERT _Static_assert
#define DW_ALIGNOF _Alignof
#endif

#define DW_ABI_VERSION UINT32_C(0)

/* Signed native status value; zero is success and negative values are failures. */
typedef int32_t DwStatus;

DW_STATIC_ASSERT(sizeof(DwStatus) == 4, "DwStatus size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwStatus) == 4, "DwStatus alignment");

/* Opaque process-local handle value; only zero has an ABI-visible meaning. */
typedef uint64_t DwHandle;

DW_STATIC_ASSERT(sizeof(DwHandle) == 8, "DwHandle size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwHandle) == 8, "DwHandle alignment");

/* Bitset of authority held through a handle. */
typedef uint64_t DwRights;

DW_STATIC_ASSERT(sizeof(DwRights) == 8, "DwRights size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwRights) == 8, "DwRights alignment");

/* Open numeric namespace identifying a kernel object type. */
typedef uint32_t DwObjectType;

DW_STATIC_ASSERT(sizeof(DwObjectType) == 4, "DwObjectType size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwObjectType) == 4, "DwObjectType alignment");

/* Bitset of observable wait signals. */
typedef uint64_t DwSignals;

DW_STATIC_ASSERT(sizeof(DwSignals) == 8, "DwSignals size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwSignals) == 8, "DwSignals alignment");

/* Untrusted userspace virtual address transported as a fixed-width scalar. */
typedef uint64_t DwUserAddress;

DW_STATIC_ASSERT(sizeof(DwUserAddress) == 8, "DwUserAddress size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwUserAddress) == 8, "DwUserAddress alignment");

/* Absolute monotonic deadline in nanoseconds. */
typedef uint64_t DwDeadline;

DW_STATIC_ASSERT(sizeof(DwDeadline) == 8, "DwDeadline size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwDeadline) == 8, "DwDeadline alignment");

/* Open numeric namespace for channel handle-transfer behavior. */
typedef uint32_t DwHandleTransferOperation;

DW_STATIC_ASSERT(sizeof(DwHandleTransferOperation) == 4, "DwHandleTransferOperation size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwHandleTransferOperation) == 4, "DwHandleTransferOperation alignment");

/* Explicit native syscall identifier; zero is invalid. */
typedef uint32_t DwSyscallId;

DW_STATIC_ASSERT(sizeof(DwSyscallId) == 4, "DwSyscallId size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwSyscallId) == 4, "DwSyscallId alignment");

/* Fixed-width byte size transported through the native ABI. */
typedef uint64_t DwSize;

DW_STATIC_ASSERT(sizeof(DwSize) == 8, "DwSize size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwSize) == 8, "DwSize alignment");

/* Fixed-width byte offset transported through the native ABI. */
typedef uint64_t DwOffset;

DW_STATIC_ASSERT(sizeof(DwOffset) == 8, "DwOffset size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwOffset) == 8, "DwOffset alignment");

/* Bitset of requested userspace mapping protections; distinct from handle rights. */
typedef uint32_t DwMemoryProtection;

DW_STATIC_ASSERT(sizeof(DwMemoryProtection) == 4, "DwMemoryProtection size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwMemoryProtection) == 4, "DwMemoryProtection alignment");

/* Bitset of MemoryObject creation options; ABI 0 supports no nonzero bits. */
typedef uint32_t DwMemoryObjectCreateFlags;

DW_STATIC_ASSERT(sizeof(DwMemoryObjectCreateFlags) == 4, "DwMemoryObjectCreateFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwMemoryObjectCreateFlags) == 4, "DwMemoryObjectCreateFlags alignment");

/* Bitset controlling AddressRegion mapping placement. */
typedef uint32_t DwAddressRegionMapFlags;

DW_STATIC_ASSERT(sizeof(DwAddressRegionMapFlags) == 4, "DwAddressRegionMapFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwAddressRegionMapFlags) == 4, "DwAddressRegionMapFlags alignment");

/* Explicit native clock-domain identifier. */
typedef uint32_t DwClockId;

DW_STATIC_ASSERT(sizeof(DwClockId) == 4, "DwClockId size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwClockId) == 4, "DwClockId alignment");

/* Open numeric namespace for task lifecycle state. */
typedef uint32_t DwTaskState;

DW_STATIC_ASSERT(sizeof(DwTaskState) == 4, "DwTaskState size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwTaskState) == 4, "DwTaskState alignment");

/* Structured task termination reason. */
typedef uint32_t DwTerminationReason;

DW_STATIC_ASSERT(sizeof(DwTerminationReason) == 4, "DwTerminationReason size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwTerminationReason) == 4, "DwTerminationReason alignment");

/* Structured exception classification; delivery is deferred beyond DW0-A. */
typedef uint32_t DwExceptionType;

DW_STATIC_ASSERT(sizeof(DwExceptionType) == 4, "DwExceptionType size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwExceptionType) == 4, "DwExceptionType alignment");

/* Presence and feature flags for DwBootInfoV1. */
typedef uint64_t DwBootInfoFlags;

DW_STATIC_ASSERT(sizeof(DwBootInfoFlags) == 8, "DwBootInfoFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootInfoFlags) == 8, "DwBootInfoFlags alignment");

/* Deepwyrm-owned normalized physical memory classification. */
typedef uint32_t DwBootMemoryKind;

DW_STATIC_ASSERT(sizeof(DwBootMemoryKind) == 4, "DwBootMemoryKind size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootMemoryKind) == 4, "DwBootMemoryKind alignment");

/* Open numeric namespace identifying a loaded boot module. */
typedef uint32_t DwBootModuleKind;

DW_STATIC_ASSERT(sizeof(DwBootModuleKind) == 4, "DwBootModuleKind size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootModuleKind) == 4, "DwBootModuleKind alignment");

/* Properties of a loaded boot module. */
typedef uint32_t DwBootModuleFlags;

DW_STATIC_ASSERT(sizeof(DwBootModuleFlags) == 4, "DwBootModuleFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootModuleFlags) == 4, "DwBootModuleFlags alignment");

/* Flags for the kernel-internal x86_64 paging-handoff carrier; ABI V1 supports no nonzero bits. */
typedef uint32_t DwBootX86_64PagingHandoffFlags;

DW_STATIC_ASSERT(sizeof(DwBootX86_64PagingHandoffFlags) == 4, "DwBootX86_64PagingHandoffFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootX86_64PagingHandoffFlags) == 4, "DwBootX86_64PagingHandoffFlags alignment");

/* Properties of a framebuffer descriptor. */
typedef uint32_t DwBootFramebufferFlags;

DW_STATIC_ASSERT(sizeof(DwBootFramebufferFlags) == 4, "DwBootFramebufferFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootFramebufferFlags) == 4, "DwBootFramebufferFlags alignment");

/* Open numeric namespace for framebuffer pixel layout. */
typedef uint32_t DwBootPixelFormat;

DW_STATIC_ASSERT(sizeof(DwBootPixelFormat) == 4, "DwBootPixelFormat size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootPixelFormat) == 4, "DwBootPixelFormat alignment");

/* Open numeric namespace identifying firmware entropy provenance. */
typedef uint32_t DwBootEntropySource;

DW_STATIC_ASSERT(sizeof(DwBootEntropySource) == 4, "DwBootEntropySource size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootEntropySource) == 4, "DwBootEntropySource alignment");

/* Properties of a firmware entropy descriptor. */
typedef uint32_t DwBootEntropyFlags;

DW_STATIC_ASSERT(sizeof(DwBootEntropyFlags) == 4, "DwBootEntropyFlags size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootEntropyFlags) == 4, "DwBootEntropyFlags alignment");

/* The operation completed successfully. */
#define DW_STATUS_SUCCESS ((DwStatus)(0))
/* An argument, flag, size, version, or reserved field is invalid. */
#define DW_STATUS_INVALID_ARGUMENT ((DwStatus)(-1))
/* A handle value is invalid or stale in the calling process. */
#define DW_STATUS_BAD_HANDLE ((DwStatus)(-2))
/* A handle refers to an object type not accepted by the operation. */
#define DW_STATUS_WRONG_OBJECT_TYPE ((DwStatus)(-3))
/* The caller lacks the required handle rights or authority. */
#define DW_STATUS_ACCESS_DENIED ((DwStatus)(-4))
/* The target object is not in a state that permits the operation. */
#define DW_STATUS_BAD_STATE ((DwStatus)(-5))
/* The requested object or entry does not exist. */
#define DW_STATUS_NOT_FOUND ((DwStatus)(-6))
/* The requested object or entry already exists. */
#define DW_STATUS_ALREADY_EXISTS ((DwStatus)(-7))
/* The operation cannot complete without blocking. */
#define DW_STATUS_WOULD_BLOCK ((DwStatus)(-8))
/* The absolute deadline elapsed before completion. */
#define DW_STATUS_TIMED_OUT ((DwStatus)(-9))
/* The operation was interrupted before completion. */
#define DW_STATUS_INTERRUPTED ((DwStatus)(-10))
/* The peer endpoint has closed. */
#define DW_STATUS_PEER_CLOSED ((DwStatus)(-11))
/* Required memory could not be allocated. */
#define DW_STATUS_NO_MEMORY ((DwStatus)(-12))
/* A bounded kernel resource is exhausted. */
#define DW_STATUS_NO_RESOURCES ((DwStatus)(-13))
/* The requested operation or optional feature is unsupported. */
#define DW_STATUS_NOT_SUPPORTED ((DwStatus)(-14))
/* A supplied output buffer is too small; required size is reported where defined. */
#define DW_STATUS_BUFFER_TOO_SMALL ((DwStatus)(-15))
/* A userspace address or range is invalid or inaccessible. */
#define DW_STATUS_BAD_ADDRESS ((DwStatus)(-16))

/* Read object state or payload. */
#define DW_RIGHT_READ ((DwRights)(1))
/* Write object state or payload. */
#define DW_RIGHT_WRITE ((DwRights)(2))
/* Use executable content or start executable state. */
#define DW_RIGHT_EXECUTE ((DwRights)(4))
/* Map backing memory into an address region. */
#define DW_RIGHT_MAP ((DwRights)(8))
/* Wait for object signals. */
#define DW_RIGHT_WAIT ((DwRights)(16))
/* Change object signal state where supported. */
#define DW_RIGHT_SIGNAL ((DwRights)(32))
/* Duplicate the handle with equal or reduced nonzero rights. */
#define DW_RIGHT_DUPLICATE ((DwRights)(64))
/* Transfer the handle through an operation that accepts handles. */
#define DW_RIGHT_TRANSFER ((DwRights)(128))
/* Query structured object information. */
#define DW_RIGHT_INSPECT ((DwRights)(256))
/* Change object configuration or lifecycle state. */
#define DW_RIGHT_MODIFY ((DwRights)(512))

/* No object type; never identifies a live kernel object. */
#define DW_OBJECT_TYPE_NONE ((DwObjectType)(0))
/* Hierarchical task and future resource-accounting boundary. */
#define DW_OBJECT_TYPE_TASK_GROUP ((DwObjectType)(1))
/* Native process object. */
#define DW_OBJECT_TYPE_PROCESS ((DwObjectType)(2))
/* Native thread object. */
#define DW_OBJECT_TYPE_THREAD ((DwObjectType)(3))
/* Page-backed and shareable memory object. */
#define DW_OBJECT_TYPE_MEMORY_OBJECT ((DwObjectType)(4))
/* Region of a process virtual address space. */
#define DW_OBJECT_TYPE_ADDRESS_REGION ((DwObjectType)(5))
/* One endpoint of a buffered bidirectional channel. */
#define DW_OBJECT_TYPE_CHANNEL ((DwObjectType)(6))
/* Explicitly signaled waitable event. */
#define DW_OBJECT_TYPE_EVENT ((DwObjectType)(7))
/* Absolute-monotonic-deadline timer. */
#define DW_OBJECT_TYPE_TIMER ((DwObjectType)(8))
/* Reserved for a future interrupt-delivery object. */
#define DW_OBJECT_TYPE_INTERRUPT ((DwObjectType)(16))
/* Reserved for future rights-scoped device resources. */
#define DW_OBJECT_TYPE_DEVICE_RESOURCE ((DwObjectType)(17))
/* Reserved for future structured exception delivery. */
#define DW_OBJECT_TYPE_EXCEPTION ((DwObjectType)(18))

/* At least one channel message can be received. */
#define DW_SIGNAL_READABLE ((DwSignals)(1))
/* Some queue capacity exists; this level signal does not reserve space or guarantee that an arbitrary racing send will fit. */
#define DW_SIGNAL_WRITABLE ((DwSignals)(2))
/* The peer channel endpoint has closed. */
#define DW_SIGNAL_PEER_CLOSED ((DwSignals)(4))
/* The process or thread reached its terminal state. */
#define DW_SIGNAL_EXITED ((DwSignals)(8))
/* The event or timer is signaled. */
#define DW_SIGNAL_SIGNALED ((DwSignals)(16))

/* The embedded framebuffer descriptor is present and must validate. */
#define DW_BOOT_INFO_FLAG_FRAMEBUFFER_PRESENT ((DwBootInfoFlags)(1))

/* Invalid or unspecified classification. */
#define DW_BOOT_MEMORY_KIND_UNSPECIFIED ((DwBootMemoryKind)(0))
/* Physical memory available for Deepwyrm after protected ranges are excluded. */
#define DW_BOOT_MEMORY_KIND_USABLE ((DwBootMemoryKind)(1))
/* Reserved memory that must not be allocated. */
#define DW_BOOT_MEMORY_KIND_RESERVED ((DwBootMemoryKind)(2))
/* ACPI reclaimable memory; reclaim policy belongs to the kernel. */
#define DW_BOOT_MEMORY_KIND_ACPI_RECLAIM ((DwBootMemoryKind)(3))
/* ACPI nonvolatile storage that must be preserved. */
#define DW_BOOT_MEMORY_KIND_ACPI_NVS ((DwBootMemoryKind)(4))
/* Memory-mapped I/O or firmware-described device address space. */
#define DW_BOOT_MEMORY_KIND_MMIO ((DwBootMemoryKind)(5))
/* Firmware runtime-service memory; Deepwyrm does not call it in DW0 but preserves it. */
#define DW_BOOT_MEMORY_KIND_RUNTIME_SERVICES ((DwBootMemoryKind)(6))
/* Memory reported as unusable or failed. */
#define DW_BOOT_MEMORY_KIND_UNUSABLE ((DwBootMemoryKind)(7))

/* Invalid or unspecified module kind. */
#define DW_BOOT_MODULE_KIND_UNSPECIFIED ((DwBootModuleKind)(0))
/* Primordial fully-static Wyrmroot bootstrap ELF. */
#define DW_BOOT_MODULE_KIND_WYRMROOT_BOOTSTRAP ((DwBootModuleKind)(1))
/* Read-only Wyrmroot bootfs image. */
#define DW_BOOT_MODULE_KIND_WYRMROOT_BOOTFS ((DwBootModuleKind)(2))
/* Exactly one READ_ONLY kernel-internal DwBootX86_64PagingHandoffV1 carrier; it must never be transferred to userspace. */
#define DW_BOOT_MODULE_KIND_DEEPWYRM_X86_64_PAGING_HANDOFF_V1 ((DwBootModuleKind)(3))

/* The module must only be exposed to userspace with read-only authority. */
#define DW_BOOT_MODULE_FLAG_READ_ONLY ((DwBootModuleFlags)(1))

/* Invalid or unspecified pixel format. */
#define DW_BOOT_PIXEL_FORMAT_UNSPECIFIED ((DwBootPixelFormat)(0))
/* Eight-bit red, green, blue, and reserved channels in byte order. */
#define DW_BOOT_PIXEL_FORMAT_RGBX8 ((DwBootPixelFormat)(1))
/* Eight-bit blue, green, red, and reserved channels in byte order. */
#define DW_BOOT_PIXEL_FORMAT_BGRX8 ((DwBootPixelFormat)(2))
/* Channel layout is described by the four mask fields. */
#define DW_BOOT_PIXEL_FORMAT_BITMASK ((DwBootPixelFormat)(3))

/* The framebuffer is a directly addressable linear pixel buffer. */
#define DW_BOOT_FRAMEBUFFER_FLAG_LINEAR ((DwBootFramebufferFlags)(1))

/* Invalid or unspecified entropy provenance. */
#define DW_BOOT_ENTROPY_SOURCE_UNSPECIFIED ((DwBootEntropySource)(0))
/* Bytes obtained from the UEFI RNG protocol. */
#define DW_BOOT_ENTROPY_SOURCE_UEFI_RNG_PROTOCOL ((DwBootEntropySource)(1))
/* Bytes obtained from another documented firmware/platform facility. */
#define DW_BOOT_ENTROPY_SOURCE_FIRMWARE_PLATFORM ((DwBootEntropySource)(2))
/* Bytes combined from multiple firmware sources by the loader. */
#define DW_BOOT_ENTROPY_SOURCE_MIXED_FIRMWARE ((DwBootEntropySource)(3))

/* The provider reports that the bytes are conditioned entropy output. */
#define DW_BOOT_ENTROPY_FLAG_CONDITIONED ((DwBootEntropyFlags)(1))

/* Mask of every DwRights bit known to ABI 0. */
#define DW_RIGHTS_KNOWN_MASK ((DwRights)(1023))
/* Compatible rights for the TASK_GROUP object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_TASK_GROUP ((DwRights)(960))
/* Compatible rights for the PROCESS object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_PROCESS ((DwRights)(976))
/* Compatible rights for the THREAD object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_THREAD ((DwRights)(980))
/* Compatible rights for the MEMORY_OBJECT object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_MEMORY_OBJECT ((DwRights)(463))
/* Compatible rights for the ADDRESS_REGION object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_ADDRESS_REGION ((DwRights)(968))
/* Compatible rights for the CHANNEL object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_CHANNEL ((DwRights)(467))
/* Compatible rights for the EVENT object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_EVENT ((DwRights)(496))
/* Compatible rights for the TIMER object type. */
#define DW_OBJECT_COMPATIBLE_RIGHTS_TIMER ((DwRights)(976))

static inline DwRights dw_object_compatible_rights(DwObjectType object_type) {
    switch (object_type) {
    case DW_OBJECT_TYPE_TASK_GROUP: return DW_OBJECT_COMPATIBLE_RIGHTS_TASK_GROUP;
    case DW_OBJECT_TYPE_PROCESS: return DW_OBJECT_COMPATIBLE_RIGHTS_PROCESS;
    case DW_OBJECT_TYPE_THREAD: return DW_OBJECT_COMPATIBLE_RIGHTS_THREAD;
    case DW_OBJECT_TYPE_MEMORY_OBJECT: return DW_OBJECT_COMPATIBLE_RIGHTS_MEMORY_OBJECT;
    case DW_OBJECT_TYPE_ADDRESS_REGION: return DW_OBJECT_COMPATIBLE_RIGHTS_ADDRESS_REGION;
    case DW_OBJECT_TYPE_CHANNEL: return DW_OBJECT_COMPATIBLE_RIGHTS_CHANNEL;
    case DW_OBJECT_TYPE_EVENT: return DW_OBJECT_COMPATIBLE_RIGHTS_EVENT;
    case DW_OBJECT_TYPE_TIMER: return DW_OBJECT_COMPATIBLE_RIGHTS_TIMER;
    default: return (DwRights)0;
    }
}

static inline int dw_rights_are_known(DwRights rights) {
    return (rights & ~DW_RIGHTS_KNOWN_MASK) == 0;
}

static inline int dw_rights_are_compatible(DwObjectType object_type, DwRights rights) {
    DwRights compatible = dw_object_compatible_rights(object_type);
    return (rights & ~compatible) == 0;
}

/* DW0 base page size in bytes for BootInfo memory ranges and handoff mappings. */
#define DW_BOOT_BASE_PAGE_SIZE ((uint32_t)(4096))
/* Required version value for DwBootMemoryRangeV1 records. */
#define DW_BOOT_MEMORY_RANGE_V1_VERSION ((uint32_t)(1))
/* Required version value for DwBootModuleV1 records. */
#define DW_BOOT_MODULE_V1_VERSION ((uint32_t)(1))
/* Required version value for DwBootX86_64PagingHandoffV1. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_V1_VERSION ((uint32_t)(1))
/* DwBootX86_64PagingHandoffV1 supports no nonzero flags. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_FLAGS_SUPPORTED_MASK ((DwBootX86_64PagingHandoffFlags)(0))
/* Deepwyrm x86_64 paging-layout contract version bound by the V1 carrier. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_LAYOUT_VERSION ((uint32_t)(2))
/* Layout-v2 temporary virtual address whose four-level path the handoff identifies. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_TEMPORARY_VIRTUAL_ADDRESS ((uint64_t)(18446742974197923840))
/* PML4 index derived from the layout-v2 temporary virtual address. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_PML4_INDEX ((uint16_t)(510))
/* PDPT index derived from the layout-v2 temporary virtual address. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_PDPT_INDEX ((uint16_t)(0))
/* Page-directory index derived from the layout-v2 temporary virtual address. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_PD_INDEX ((uint16_t)(0))
/* Page-table index derived from the layout-v2 temporary virtual address. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_PT_INDEX ((uint16_t)(0))
/* Required module-relative byte offset of the flat table-frame list. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_TABLE_FRAMES_OFFSET ((uint32_t)(112))
/* Required byte stride of each u64 table-frame physical address. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_TABLE_FRAME_STRIDE ((uint32_t)(8))
/* Minimum list count covering the distinct CR3 root and three-child temporary path. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_MIN_TABLE_FRAME_COUNT ((uint32_t)(4))
/* Maximum number of transition page-table frames accepted in the V1 carrier. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_MAX_TABLE_FRAME_COUNT ((uint32_t)(256))
/* Maximum exact V1 carrier extent: 112-byte header plus 256 u64 frame addresses. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_MAX_BYTE_LEN ((uint32_t)(2160))
/* Minimum valid x86_64 physical-address width encoded by the V1 carrier. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_MIN_PHYSICAL_ADDRESS_WIDTH ((uint32_t)(12))
/* Maximum valid x86_64 page-table physical-address width encoded by the V1 carrier. */
#define DW_BOOT_X86_64_PAGING_HANDOFF_MAX_PHYSICAL_ADDRESS_WIDTH ((uint32_t)(52))
/* Required version value for DwBootFramebufferV1 records. */
#define DW_BOOT_FRAMEBUFFER_V1_VERSION ((uint32_t)(1))
/* Required version value for DwBootEntropyV1 records. */
#define DW_BOOT_ENTROPY_V1_VERSION ((uint32_t)(1))
/* Required version value for DwBootInfoV1 records. */
#define DW_BOOT_INFO_V1_VERSION ((uint32_t)(1))
/* The only ABI-visible invalid handle value. */
#define DW_HANDLE_INVALID ((DwHandle)(0))
/* Request immediate deadline evaluation. */
#define DW_DEADLINE_NOW ((DwDeadline)(0))
/* No finite monotonic deadline. */
#define DW_DEADLINE_INFINITE ((DwDeadline)(18446744073709551615))
/* Canonical ABI-0 page size for MemoryObject lengths, offsets, and userspace mapping ranges; equal to the boot handoff page size and used for checked page-rounding of mappable capacity. */
#define DW_BASE_PAGE_SIZE ((uint32_t)(4096))
/* Maximum DW0 channel payload bytes. */
#define DW_CHANNEL_MAX_PAYLOAD ((uint32_t)(65536))
/* Maximum DW0 channel handles per message. */
#define DW_CHANNEL_MAX_HANDLES ((uint32_t)(16))
/* Maximum handles accepted by one ABI-0 wait_many operation. */
#define DW_WAIT_MANY_MAX_ITEMS ((uint32_t)(64))
/* Wake every waiter eligible for atomic_wake. */
#define DW_ATOMIC_WAKE_ALL ((uint32_t)(4294967295))
/* Move a handle on commit; no duplicate transfer operation exists in ABI 0. */
#define DW_HANDLE_TRANSFER_MOVE ((DwHandleTransferOperation)(1))
/* Return the lowest-index ready item. */
#define DW_WAIT_MODE_ANY ((uint32_t)(0))
/* Reserved in ABI 0; operations return NOT_SUPPORTED. */
#define DW_WAIT_MODE_ALL ((uint32_t)(1))
/* Monotonic active-time clock used by ABI-0 deadlines. */
#define DW_CLOCK_MONOTONIC_ACTIVE ((DwClockId)(0))
/* Reserved clock domain for future suspend-aware elapsed time. */
#define DW_CLOCK_BOOTTIME ((DwClockId)(1))
/* Typed object_get_info topic for basic type and rights metadata. */
#define DW_OBJECT_INFO_BASIC_V1 ((uint32_t)(1))
/* Typed object_get_info topic for process or thread termination state. */
#define DW_OBJECT_INFO_TASK_STATE_V1 ((uint32_t)(65537))
/* Typed object_get_info topic for exact logical MemoryObject byte size. */
#define DW_OBJECT_INFO_MEMORY_OBJECT_V1 ((uint32_t)(131073))
/* Permit userspace reads from a mapping. */
#define DW_MEMORY_PROTECTION_READ ((DwMemoryProtection)(1))
/* Permit userspace writes to a mapping. */
#define DW_MEMORY_PROTECTION_WRITE ((DwMemoryProtection)(2))
/* Permit userspace instruction fetches from a mapping. */
#define DW_MEMORY_PROTECTION_EXECUTE ((DwMemoryProtection)(4))
/* All mapping-protection bits recognized by ABI 0. */
#define DW_MEMORY_PROTECTION_SUPPORTED_MASK ((DwMemoryProtection)(7))
/* ABI 0 supports no nonzero MemoryObject creation flags. */
#define DW_MEMORY_OBJECT_CREATE_FLAGS_SUPPORTED_MASK ((DwMemoryObjectCreateFlags)(0))
/* Map at requested_address exactly, without replacing an existing mapping. */
#define DW_ADDRESS_REGION_MAP_FLAG_FIXED ((DwAddressRegionMapFlags)(1))
/* All AddressRegion mapping flags recognized by ABI 0. */
#define DW_ADDRESS_REGION_MAP_FLAGS_SUPPORTED_MASK ((DwAddressRegionMapFlags)(1))
/* Required version value for DwAddressRegionMapArgsV1. */
#define DW_ADDRESS_REGION_MAP_ARGS_V1_VERSION ((uint32_t)(1))
/* Task exists but has not started. */
#define DW_TASK_STATE_CREATED ((DwTaskState)(1))
/* Task has started and has not terminated. */
#define DW_TASK_STATE_RUNNING ((DwTaskState)(2))
/* Task reached a terminal state. */
#define DW_TASK_STATE_EXITED ((DwTaskState)(3))
/* Application exited normally with a 32-bit code. */
#define DW_TERMINATION_NORMAL_EXIT ((DwTerminationReason)(1))
/* Task was terminated through authorized task control. */
#define DW_TERMINATION_AUTHORIZED ((DwTerminationReason)(2))
/* Task terminated because of an unhandled structured exception. */
#define DW_TERMINATION_UNHANDLED_EXCEPTION ((DwTerminationReason)(3))
/* Task was terminated by a resource or policy decision. */
#define DW_TERMINATION_RESOURCE_POLICY ((DwTerminationReason)(4))
/* Task terminated during ancestor TaskGroup teardown. */
#define DW_TERMINATION_TASK_GROUP_TEARDOWN ((DwTerminationReason)(5))
/* No exception applies to this termination record. */
#define DW_EXCEPTION_NONE ((DwExceptionType)(0))
/* Page fault exception. */
#define DW_EXCEPTION_PAGE_FAULT ((DwExceptionType)(1))
/* Illegal instruction exception. */
#define DW_EXCEPTION_ILLEGAL_INSTRUCTION ((DwExceptionType)(2))
/* Breakpoint exception. */
#define DW_EXCEPTION_BREAKPOINT ((DwExceptionType)(3))
/* Divide error exception. */
#define DW_EXCEPTION_DIVIDE_ERROR ((DwExceptionType)(4))
/* General protection exception. */
#define DW_EXCEPTION_GENERAL_PROTECTION ((DwExceptionType)(5))
/* Debug trap exception. */
#define DW_EXCEPTION_DEBUG_TRAP ((DwExceptionType)(6))

/* Write DwAbiInfoV1 and report required size without relying on a release string. */
#define DW_SYSCALL_ABI_GET_INFO ((DwSyscallId)(UINT32_C(0x00000001)))
/* Close one caller-local handle; no right is required beyond possession. */
#define DW_SYSCALL_HANDLE_CLOSE ((DwSyscallId)(UINT32_C(0x00000010)))
/* Create a duplicate with a nonzero rights subset; zero or escalating rights fail. */
#define DW_SYSCALL_HANDLE_DUPLICATE ((DwSyscallId)(UINT32_C(0x00000011)))
/* Return structured object information; reserved input fields and undersized buffers fail closed. */
#define DW_SYSCALL_OBJECT_GET_INFO_V1 ((DwSyscallId)(UINT32_C(0x00000012)))
/* Create a child task group with nonzero, known, object-compatible requested rights. */
#define DW_SYSCALL_TASK_GROUP_CREATE ((DwSyscallId)(UINT32_C(0x00010001)))
/* Recursively terminate a task group and its descendants. */
#define DW_SYSCALL_TASK_GROUP_TERMINATE ((DwSyscallId)(UINT32_C(0x00010002)))
/* Create a process using DwProcessCreateArgsV1 and atomically install its bootstrap channel. */
#define DW_SYSCALL_PROCESS_CREATE ((DwSyscallId)(UINT32_C(0x00010010)))
/* Exit the calling process normally with a 32-bit application code. */
#define DW_SYSCALL_PROCESS_EXIT ((DwSyscallId)(UINT32_C(0x00010011)))
/* Explicitly terminate a process with structured reason metadata. */
#define DW_SYSCALL_PROCESS_TERMINATE ((DwSyscallId)(UINT32_C(0x00010012)))
/* Create a thread in CREATED state with nonzero, known, object-compatible requested rights. */
#define DW_SYSCALL_THREAD_CREATE ((DwSyscallId)(UINT32_C(0x00010020)))
/* Start a created thread using DwThreadStartArgsV1. */
#define DW_SYSCALL_THREAD_START ((DwSyscallId)(UINT32_C(0x00010021)))
/* Exit the calling thread normally with a 32-bit code. */
#define DW_SYSCALL_THREAD_EXIT ((DwSyscallId)(UINT32_C(0x00010022)))
/* Explicitly terminate a thread. */
#define DW_SYSCALL_THREAD_TERMINATE ((DwSyscallId)(UINT32_C(0x00010023)))
/* Create a zero-filled page-backed MemoryObject with an immutable size. Zero, unaligned, or overflowing byte_len and any nonzero flags are INVALID_ARGUMENT; inaccessible output storage is BAD_ADDRESS; backing exhaustion is NO_MEMORY. Requested rights must be nonzero, known, and object-compatible, and failure publishes no handle. */
#define DW_SYSCALL_MEMORY_OBJECT_CREATE ((DwSyscallId)(UINT32_C(0x00020001)))
/* Transactionally map a page-aligned MemoryObject range using exact-size DwAddressRegionMapArgsV1. Malformed size/version/reserved fields, unknown flags or protections, zero/W+X protections, unaligned or overflowing ranges, and offset plus length beyond the checked align_up(logical byte_size, DW_BASE_PAGE_SIZE) mappable capacity are INVALID_ARGUMENT. WRITE-only or EXECUTE-only is NOT_SUPPORTED on DW0 x86_64 and READ is never added implicitly. DwMemoryObjectInfoV1 and other size APIs report only exact logical byte_size. Any final-page tail between byte_size and mappable capacity is hardware-addressable padding, is zero-initialized before first publication, never contains unrelated data, and may be shared or modified subject to mapping protections. Callers must parse or use object content only through exact logical byte_size; the ABI promises no subpage access enforcement within that tail. Allocator-chosen placement requires address zero; FIXED requires an exact nonzero page-aligned lower-userspace address, never replaces, and returns ALREADY_EXISTS on overlap. Noncanonical, page-zero, kernel, or outside-region addresses are BAD_ADDRESS. Every mapping requires source MAP+READ; requested WRITE additionally requires source WRITE and requested EXECUTE requires source EXECUTE. Successful mapping captures the source handle's READ/WRITE/EXECUTE authority as the ceiling for later protect operations, including after the source handle closes; violation is ACCESS_DENIED. Failure to find allocator-chosen space returns NO_MEMORY. Every failure leaves mappings and out_address unchanged. */
#define DW_SYSCALL_ADDRESS_REGION_MAP ((DwSyscallId)(UINT32_C(0x00020010)))
/* Transactionally remove a nonempty page-aligned fully mapped userspace range. Zero, unaligned, or overflowing ranges are INVALID_ARGUMENT; noncanonical, page-zero, kernel, or outside-region addresses are BAD_ADDRESS; any unmapped hole is NOT_FOUND. Failure leaves the complete range unchanged. */
#define DW_SYSCALL_ADDRESS_REGION_UNMAP ((DwSyscallId)(UINT32_C(0x00020011)))
/* Transactionally change a nonempty page-aligned fully mapped userspace range without exceeding its captured mapping authority. Zero, unknown, or W+X protections and zero, unaligned, or overflowing ranges are INVALID_ARGUMENT; WRITE-only or EXECUTE-only is NOT_SUPPORTED on DW0 x86_64 and READ is never added implicitly. Noncanonical, page-zero, kernel, or outside-region addresses are BAD_ADDRESS; any unmapped hole is NOT_FOUND; exceeding mapping authority is ACCESS_DENIED. Failure leaves the complete range unchanged. */
#define DW_SYSCALL_ADDRESS_REGION_PROTECT ((DwSyscallId)(UINT32_C(0x00020012)))
/* Create connected endpoints with nonzero, known, object-compatible requested rights. */
#define DW_SYSCALL_CHANNEL_CREATE ((DwSyscallId)(UINT32_C(0x00030001)))
/* Atomically queue bytes and moved handles; each source needs TRANSFER, failures preserve every sender handle, and successful commit transfers object references to the queue. */
#define DW_SYSCALL_CHANNEL_SEND ((DwSyscallId)(UINT32_C(0x00030002)))
/* Receive one atomic datagram; BUFFER_TOO_SMALL consumes nothing and receiver handles are created only on success. */
#define DW_SYSCALL_CHANNEL_RECEIVE ((DwSyscallId)(UINT32_C(0x00030003)))
/* Wait for a nonzero signal mask on one handle. */
#define DW_SYSCALL_WAIT_ONE ((DwSyscallId)(UINT32_C(0x00040001)))
/* WAIT_ANY over a nonempty DwWaitItemV1 array; the lowest ready index is deterministic and WAIT_ALL is reserved. */
#define DW_SYSCALL_WAIT_MANY ((DwSyscallId)(UINT32_C(0x00040002)))
/* Create an unsignaled manual-reset event with nonzero, known, object-compatible requested rights. */
#define DW_SYSCALL_EVENT_CREATE ((DwSyscallId)(UINT32_C(0x00040010)))
/* Set or reset a manual event; only SIGNALED is valid, and empty or overlapping clear/set masks are invalid. */
#define DW_SYSCALL_EVENT_SIGNAL ((DwSyscallId)(UINT32_C(0x00040011)))
/* Wait while a validated aligned userspace u32 equals expected; mismatch returns WOULD_BLOCK and callers recheck their atomic predicate after wake. */
#define DW_SYSCALL_ATOMIC_WAIT32 ((DwSyscallId)(UINT32_C(0x00040020)))
/* Wake up to count waiters on an aligned userspace u32 address; zero is a no-op and DW_ATOMIC_WAKE_ALL wakes every eligible waiter. */
#define DW_SYSCALL_ATOMIC_WAKE ((DwSyscallId)(UINT32_C(0x00040021)))
/* Read an explicit clock domain in nanoseconds. */
#define DW_SYSCALL_CLOCK_GET ((DwSyscallId)(UINT32_C(0x00050001)))
/* Create a one-shot monotonic timer with nonzero, known, object-compatible requested rights. */
#define DW_SYSCALL_TIMER_CREATE ((DwSyscallId)(UINT32_C(0x00050010)))
/* Arm or replace a one-shot timer, atomically clearing prior signal state; INFINITE is invalid and cancel is explicit. */
#define DW_SYSCALL_TIMER_SET ((DwSyscallId)(UINT32_C(0x00050011)))
/* Cancel a one-shot timer and clear its signaled state. */
#define DW_SYSCALL_TIMER_CANCEL ((DwSyscallId)(UINT32_C(0x00050012)))

/* One normalized physical memory-map entry. */
typedef struct DwBootMemoryRangeV1 {
    /* Byte size of this entry, including any compatible trailing extension. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Normalized memory classification. */
    DwBootMemoryKind kind;
    /* Reserved; producer sets zero and consumer rejects nonzero. */
    uint32_t reserved0;
    /* Physical start address. */
    uint64_t physical_start;
    /* Number of 4096-byte pages. */
    uint64_t page_count;
    /* Raw UEFI memory attributes preserved as opaque bits. */
    uint64_t firmware_attributes;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[3];
} DwBootMemoryRangeV1;
#define DW_BOOT_MEMORY_RANGE_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwBootMemoryRangeV1) == 64, "DwBootMemoryRangeV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootMemoryRangeV1) == 8, "DwBootMemoryRangeV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, size) == 0, "DwBootMemoryRangeV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, version) == 4, "DwBootMemoryRangeV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, kind) == 8, "DwBootMemoryRangeV1.kind offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, reserved0) == 12, "DwBootMemoryRangeV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, physical_start) == 16, "DwBootMemoryRangeV1.physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, page_count) == 24, "DwBootMemoryRangeV1.page_count offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, firmware_attributes) == 32, "DwBootMemoryRangeV1.firmware_attributes offset");
DW_STATIC_ASSERT(offsetof(DwBootMemoryRangeV1, reserved) == 40, "DwBootMemoryRangeV1.reserved offset");

/* One loaded boot module supplied by the Wyrmroot EFI loader. */
typedef struct DwBootModuleV1 {
    /* Byte size of this entry, including any compatible trailing extension. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Module semantic kind. */
    DwBootModuleKind kind;
    /* Supported module properties. */
    DwBootModuleFlags flags;
    /* Physical start address of module bytes. */
    uint64_t physical_start;
    /* Exact module byte length. */
    uint64_t byte_len;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwBootModuleV1;
#define DW_BOOT_MODULE_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwBootModuleV1) == 64, "DwBootModuleV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootModuleV1) == 8, "DwBootModuleV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, size) == 0, "DwBootModuleV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, version) == 4, "DwBootModuleV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, kind) == 8, "DwBootModuleV1.kind offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, flags) == 12, "DwBootModuleV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, physical_start) == 16, "DwBootModuleV1.physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, byte_len) == 24, "DwBootModuleV1.byte_len offset");
DW_STATIC_ASSERT(offsetof(DwBootModuleV1, reserved) == 32, "DwBootModuleV1.reserved offset");

/* Fixed header for the sole READ_ONLY kernel-internal x86_64 transition-page-table carrier. The containing module's physical_start is 4096-byte aligned, begins with this header, and is followed at table_frames_offset by a flat u64 list. The list is strictly ascending and unique, contains the distinct CR3 root and three temporary-path child frames, and enumerates every current transition page-table frame exactly once with no data frames. Every physical address is nonzero, 4096-byte aligned, has no bits outside physical_address_width, and remains reserved until Deepwyrm replaces CR3. The containing module's byte_len, total_byte_len, and table_frames_offset plus table_frame_count times table_frame_stride must be exactly equal. */
typedef struct DwBootX86_64PagingHandoffV1 {
    /* Fixed header byte size; must equal DW_BOOT_X86_64_PAGING_HANDOFF_V1_SIZE. */
    uint32_t size;
    /* Structure version; must equal DW_BOOT_X86_64_PAGING_HANDOFF_V1_VERSION. */
    uint32_t version;
    /* Must contain only supported bits; V1 requires zero. */
    DwBootX86_64PagingHandoffFlags flags;
    /* MAXPHYADDR width in bits; must be within the V1 bounds and cover every encoded physical address. */
    uint32_t physical_address_width;
    /* Exact current CR3 root-frame physical address with its low 12 bits zero. */
    uint64_t cr3_root_physical;
    /* Module-relative list offset; must equal the V1 table-frames-offset constant. */
    uint32_t table_frames_offset;
    /* Strictly bounded flat-list count from the V1 minimum through maximum inclusive. */
    uint32_t table_frame_count;
    /* List element byte stride; must equal the V1 u64 stride constant. */
    uint32_t table_frame_stride;
    /* Exact complete carrier extent, including the header and flat list. */
    uint32_t total_byte_len;
    /* Must equal the accepted Deepwyrm x86_64 paging-layout version. */
    uint32_t paging_layout_version;
    /* Reserved; producer sets zero and consumer rejects nonzero. */
    uint32_t reserved0;
    /* Must equal the layout-v2 temporary virtual-address constant. */
    uint64_t temporary_virtual_address;
    /* Must equal the PML4 index derived from temporary_virtual_address. */
    uint16_t pml4_index;
    /* Must equal the PDPT index derived from temporary_virtual_address. */
    uint16_t pdpt_index;
    /* Must equal the page-directory index derived from temporary_virtual_address. */
    uint16_t pd_index;
    /* Must equal the page-table index derived from temporary_virtual_address. */
    uint16_t pt_index;
    /* Distinct PML4 child frame selected by pml4_index; low 12 bits must be zero. */
    uint64_t temporary_pdpt_frame_physical;
    /* Distinct PDPT child frame selected by pdpt_index; low 12 bits must be zero. */
    uint64_t temporary_pd_frame_physical;
    /* Distinct page-directory child frame selected by pd_index; low 12 bits must be zero. */
    uint64_t temporary_pt_frame_physical;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[3];
} DwBootX86_64PagingHandoffV1;
#define DW_BOOT_X86_64_PAGING_HANDOFF_V1_SIZE ((uint32_t)(112))
DW_STATIC_ASSERT(sizeof(DwBootX86_64PagingHandoffV1) == 112, "DwBootX86_64PagingHandoffV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootX86_64PagingHandoffV1) == 8, "DwBootX86_64PagingHandoffV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, size) == 0, "DwBootX86_64PagingHandoffV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, version) == 4, "DwBootX86_64PagingHandoffV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, flags) == 8, "DwBootX86_64PagingHandoffV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, physical_address_width) == 12, "DwBootX86_64PagingHandoffV1.physical_address_width offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, cr3_root_physical) == 16, "DwBootX86_64PagingHandoffV1.cr3_root_physical offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, table_frames_offset) == 24, "DwBootX86_64PagingHandoffV1.table_frames_offset offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, table_frame_count) == 28, "DwBootX86_64PagingHandoffV1.table_frame_count offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, table_frame_stride) == 32, "DwBootX86_64PagingHandoffV1.table_frame_stride offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, total_byte_len) == 36, "DwBootX86_64PagingHandoffV1.total_byte_len offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, paging_layout_version) == 40, "DwBootX86_64PagingHandoffV1.paging_layout_version offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, reserved0) == 44, "DwBootX86_64PagingHandoffV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, temporary_virtual_address) == 48, "DwBootX86_64PagingHandoffV1.temporary_virtual_address offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, pml4_index) == 56, "DwBootX86_64PagingHandoffV1.pml4_index offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, pdpt_index) == 58, "DwBootX86_64PagingHandoffV1.pdpt_index offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, pd_index) == 60, "DwBootX86_64PagingHandoffV1.pd_index offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, pt_index) == 62, "DwBootX86_64PagingHandoffV1.pt_index offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, temporary_pdpt_frame_physical) == 64, "DwBootX86_64PagingHandoffV1.temporary_pdpt_frame_physical offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, temporary_pd_frame_physical) == 72, "DwBootX86_64PagingHandoffV1.temporary_pd_frame_physical offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, temporary_pt_frame_physical) == 80, "DwBootX86_64PagingHandoffV1.temporary_pt_frame_physical offset");
DW_STATIC_ASSERT(offsetof(DwBootX86_64PagingHandoffV1, reserved) == 88, "DwBootX86_64PagingHandoffV1.reserved offset");

/* Optional linear framebuffer description embedded in DwBootInfoV1. */
typedef struct DwBootFramebufferV1 {
    /* Byte size of this descriptor. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Supported framebuffer properties. */
    DwBootFramebufferFlags flags;
    /* Pixel byte/mask layout. */
    DwBootPixelFormat pixel_format;
    /* Physical framebuffer base. */
    uint64_t physical_start;
    /* Mapped framebuffer byte length. */
    uint64_t byte_len;
    /* Visible width in pixels. */
    uint32_t width;
    /* Visible height in pixels. */
    uint32_t height;
    /* Pixel stride between scanlines. */
    uint32_t pixels_per_scanline;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Red channel bit mask for BITMASK format, otherwise zero. */
    uint32_t red_mask;
    /* Green channel bit mask for BITMASK format, otherwise zero. */
    uint32_t green_mask;
    /* Blue channel bit mask for BITMASK format, otherwise zero. */
    uint32_t blue_mask;
    /* Reserved channel bit mask for BITMASK format, otherwise zero. */
    uint32_t reserved_mask;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwBootFramebufferV1;
#define DW_BOOT_FRAMEBUFFER_V1_SIZE ((uint32_t)(96))
DW_STATIC_ASSERT(sizeof(DwBootFramebufferV1) == 96, "DwBootFramebufferV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootFramebufferV1) == 8, "DwBootFramebufferV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, size) == 0, "DwBootFramebufferV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, version) == 4, "DwBootFramebufferV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, flags) == 8, "DwBootFramebufferV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, pixel_format) == 12, "DwBootFramebufferV1.pixel_format offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, physical_start) == 16, "DwBootFramebufferV1.physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, byte_len) == 24, "DwBootFramebufferV1.byte_len offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, width) == 32, "DwBootFramebufferV1.width offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, height) == 36, "DwBootFramebufferV1.height offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, pixels_per_scanline) == 40, "DwBootFramebufferV1.pixels_per_scanline offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, reserved0) == 44, "DwBootFramebufferV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, red_mask) == 48, "DwBootFramebufferV1.red_mask offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, green_mask) == 52, "DwBootFramebufferV1.green_mask offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, blue_mask) == 56, "DwBootFramebufferV1.blue_mask offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, reserved_mask) == 60, "DwBootFramebufferV1.reserved_mask offset");
DW_STATIC_ASSERT(offsetof(DwBootFramebufferV1, reserved) == 64, "DwBootFramebufferV1.reserved offset");

/* Optional firmware entropy bytes embedded in DwBootInfoV1. */
typedef struct DwBootEntropyV1 {
    /* Byte size of this descriptor. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Entropy provenance. */
    DwBootEntropySource source;
    /* Supported entropy properties. */
    DwBootEntropyFlags flags;
    /* Physical start address of entropy bytes. */
    uint64_t physical_start;
    /* Number of entropy bytes. */
    uint64_t byte_len;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwBootEntropyV1;
#define DW_BOOT_ENTROPY_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwBootEntropyV1) == 64, "DwBootEntropyV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootEntropyV1) == 8, "DwBootEntropyV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, size) == 0, "DwBootEntropyV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, version) == 4, "DwBootEntropyV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, source) == 8, "DwBootEntropyV1.source offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, flags) == 12, "DwBootEntropyV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, physical_start) == 16, "DwBootEntropyV1.physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, byte_len) == 24, "DwBootEntropyV1.byte_len offset");
DW_STATIC_ASSERT(offsetof(DwBootEntropyV1, reserved) == 32, "DwBootEntropyV1.reserved offset");

/* Physical loader-to-kernel handoff produced only after ExitBootServices succeeds. */
typedef struct DwBootInfoV1 {
    /* Byte size of this structure, including compatible trailing extensions. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Presence and feature flags. */
    DwBootInfoFlags flags;
    /* Physical address of the memory-entry table. */
    uint64_t memory_map_physical_start;
    /* Number of memory entries. */
    uint64_t memory_map_entry_count;
    /* Byte stride of each memory entry. */
    uint32_t memory_map_entry_size;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Physical address of the module-entry table. */
    uint64_t modules_physical_start;
    /* Number of module entries. */
    uint64_t module_count;
    /* Byte stride of each module entry. */
    uint32_t module_entry_size;
    /* Reserved; must be zero. */
    uint32_t reserved1;
    /* Physical RSDP address, or zero when absent. */
    uint64_t acpi_rsdp_physical_address;
    /* Optional framebuffer descriptor controlled by flags. */
    DwBootFramebufferV1 framebuffer;
    /* Physical address of opaque command-line bytes. */
    uint64_t command_line_physical_start;
    /* Number of command-line bytes; no implicit terminator. */
    uint64_t command_line_byte_len;
    /* Optional firmware entropy descriptor; all-zero means absent. */
    DwBootEntropyV1 entropy;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[8];
} DwBootInfoV1;
#define DW_BOOT_INFO_V1_SIZE ((uint32_t)(312))
DW_STATIC_ASSERT(sizeof(DwBootInfoV1) == 312, "DwBootInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwBootInfoV1) == 8, "DwBootInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, size) == 0, "DwBootInfoV1.size offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, version) == 4, "DwBootInfoV1.version offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, flags) == 8, "DwBootInfoV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, memory_map_physical_start) == 16, "DwBootInfoV1.memory_map_physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, memory_map_entry_count) == 24, "DwBootInfoV1.memory_map_entry_count offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, memory_map_entry_size) == 32, "DwBootInfoV1.memory_map_entry_size offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, reserved0) == 36, "DwBootInfoV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, modules_physical_start) == 40, "DwBootInfoV1.modules_physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, module_count) == 48, "DwBootInfoV1.module_count offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, module_entry_size) == 56, "DwBootInfoV1.module_entry_size offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, reserved1) == 60, "DwBootInfoV1.reserved1 offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, acpi_rsdp_physical_address) == 64, "DwBootInfoV1.acpi_rsdp_physical_address offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, framebuffer) == 72, "DwBootInfoV1.framebuffer offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, command_line_physical_start) == 168, "DwBootInfoV1.command_line_physical_start offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, command_line_byte_len) == 176, "DwBootInfoV1.command_line_byte_len offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, entropy) == 184, "DwBootInfoV1.entropy offset");
DW_STATIC_ASSERT(offsetof(DwBootInfoV1, reserved) == 248, "DwBootInfoV1.reserved offset");

/* Kernel ABI and bounded implementation-limit discovery. */
typedef struct DwAbiInfoV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Native ABI version; zero for DW0. */
    uint32_t abi_version;
    /* Base page size in bytes. */
    uint32_t page_size;
    /* Open feature bitset; unknown bits remain representable. */
    uint64_t feature_bits;
    /* Maximum channel payload bytes. */
    uint32_t max_channel_payload;
    /* Maximum handles per channel message. */
    uint32_t max_channel_handles;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwAbiInfoV1;
#define DW_ABI_INFO_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwAbiInfoV1) == 64, "DwAbiInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwAbiInfoV1) == 8, "DwAbiInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, size) == 0, "DwAbiInfoV1.size offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, version) == 4, "DwAbiInfoV1.version offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, abi_version) == 8, "DwAbiInfoV1.abi_version offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, page_size) == 12, "DwAbiInfoV1.page_size offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, feature_bits) == 16, "DwAbiInfoV1.feature_bits offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, max_channel_payload) == 24, "DwAbiInfoV1.max_channel_payload offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, max_channel_handles) == 28, "DwAbiInfoV1.max_channel_handles offset");
DW_STATIC_ASSERT(offsetof(DwAbiInfoV1, reserved) == 32, "DwAbiInfoV1.reserved offset");

/* Basic rights-controlled object information returned by object_get_info_v1. */
typedef struct DwObjectInfoV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Open object-type namespace value. */
    DwObjectType object_type;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Rights held by the queried handle. */
    DwRights rights;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwObjectInfoV1;
#define DW_OBJECT_INFO_V1_SIZE ((uint32_t)(56))
DW_STATIC_ASSERT(sizeof(DwObjectInfoV1) == 56, "DwObjectInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwObjectInfoV1) == 8, "DwObjectInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, size) == 0, "DwObjectInfoV1.size offset");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, version) == 4, "DwObjectInfoV1.version offset");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, object_type) == 8, "DwObjectInfoV1.object_type offset");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, reserved0) == 12, "DwObjectInfoV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, rights) == 16, "DwObjectInfoV1.rights offset");
DW_STATIC_ASSERT(offsetof(DwObjectInfoV1, reserved) == 24, "DwObjectInfoV1.reserved offset");

/* Structured process or thread termination information. */
typedef struct DwTaskTerminationInfoV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Current task lifecycle state. */
    DwTaskState state;
    /* Termination classification, or zero before exit. */
    DwTerminationReason reason;
    /* Application-defined 32-bit normal-exit code. */
    uint32_t application_code;
    /* Structured exception classification when applicable. */
    DwExceptionType exception_type;
    /* Reason-specific detail value. */
    uint32_t detail;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Faulting address when applicable, otherwise zero. */
    uint64_t fault_address;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[3];
} DwTaskTerminationInfoV1;
#define DW_TASK_TERMINATION_INFO_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwTaskTerminationInfoV1) == 64, "DwTaskTerminationInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwTaskTerminationInfoV1) == 8, "DwTaskTerminationInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, size) == 0, "DwTaskTerminationInfoV1.size offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, version) == 4, "DwTaskTerminationInfoV1.version offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, state) == 8, "DwTaskTerminationInfoV1.state offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, reason) == 12, "DwTaskTerminationInfoV1.reason offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, application_code) == 16, "DwTaskTerminationInfoV1.application_code offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, exception_type) == 20, "DwTaskTerminationInfoV1.exception_type offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, detail) == 24, "DwTaskTerminationInfoV1.detail offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, reserved0) == 28, "DwTaskTerminationInfoV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, fault_address) == 32, "DwTaskTerminationInfoV1.fault_address offset");
DW_STATIC_ASSERT(offsetof(DwTaskTerminationInfoV1, reserved) == 40, "DwTaskTerminationInfoV1.reserved offset");

/* Read-only exact logical MemoryObject size information. */
typedef struct DwMemoryObjectInfoV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Exact logical content byte size, fixed for the object lifetime; APIs report this value rather than page-rounded mappable capacity. */
    uint64_t byte_size;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[2];
} DwMemoryObjectInfoV1;
#define DW_MEMORY_OBJECT_INFO_V1_SIZE ((uint32_t)(32))
DW_STATIC_ASSERT(sizeof(DwMemoryObjectInfoV1) == 32, "DwMemoryObjectInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwMemoryObjectInfoV1) == 8, "DwMemoryObjectInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwMemoryObjectInfoV1, size) == 0, "DwMemoryObjectInfoV1.size offset");
DW_STATIC_ASSERT(offsetof(DwMemoryObjectInfoV1, version) == 4, "DwMemoryObjectInfoV1.version offset");
DW_STATIC_ASSERT(offsetof(DwMemoryObjectInfoV1, byte_size) == 8, "DwMemoryObjectInfoV1.byte_size offset");
DW_STATIC_ASSERT(offsetof(DwMemoryObjectInfoV1, reserved) == 16, "DwMemoryObjectInfoV1.reserved offset");

/* Versioned arguments selecting one page-aligned MemoryObject mapping. */
typedef struct DwAddressRegionMapArgsV1 {
    /* Byte size of this structure; must equal the V1 size in ABI 0. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Page-aligned byte offset into the MemoryObject's page-rounded mappable capacity. */
    DwOffset memory_object_offset;
    /* Nonzero page-aligned mapping length; offset plus length must not exceed page-rounded mappable capacity. */
    DwSize byte_len;
    /* Zero for allocator-chosen placement; exact nonzero page-aligned userspace address with FIXED. */
    DwUserAddress requested_address;
    /* Explicit nonzero protection bits; ABI-0 x86_64 supports READ, READ plus WRITE, or READ plus EXECUTE. */
    DwMemoryProtection protections;
    /* Zero for allocator-chosen placement or FIXED for exact no-replace placement. */
    DwAddressRegionMapFlags flags;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwAddressRegionMapArgsV1;
#define DW_ADDRESS_REGION_MAP_ARGS_V1_SIZE ((uint32_t)(72))
DW_STATIC_ASSERT(sizeof(DwAddressRegionMapArgsV1) == 72, "DwAddressRegionMapArgsV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwAddressRegionMapArgsV1) == 8, "DwAddressRegionMapArgsV1 alignment");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, size) == 0, "DwAddressRegionMapArgsV1.size offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, version) == 4, "DwAddressRegionMapArgsV1.version offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, memory_object_offset) == 8, "DwAddressRegionMapArgsV1.memory_object_offset offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, byte_len) == 16, "DwAddressRegionMapArgsV1.byte_len offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, requested_address) == 24, "DwAddressRegionMapArgsV1.requested_address offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, protections) == 32, "DwAddressRegionMapArgsV1.protections offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, flags) == 36, "DwAddressRegionMapArgsV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwAddressRegionMapArgsV1, reserved) == 40, "DwAddressRegionMapArgsV1.reserved offset");

/* Arguments for creating a child process and installing its bootstrap channel without a magic handle value. */
typedef struct DwProcessCreateArgsV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Parent task group requiring MODIFY. */
    DwHandle task_group;
    /* Channel endpoint requiring TRANSFER and moved into the child only on complete success. */
    DwHandle bootstrap_channel;
    /* Nonzero rights requested for the returned process handle. */
    DwRights process_rights;
    /* Nonzero rights requested for the root address-region handle. */
    DwRights root_region_rights;
    /* Nonzero subset of source rights installed in the child. */
    DwRights child_bootstrap_rights;
    /* Must be zero in ABI 0. */
    uint64_t flags;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwProcessCreateArgsV1;
#define DW_PROCESS_CREATE_ARGS_V1_SIZE ((uint32_t)(88))
DW_STATIC_ASSERT(sizeof(DwProcessCreateArgsV1) == 88, "DwProcessCreateArgsV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwProcessCreateArgsV1) == 8, "DwProcessCreateArgsV1 alignment");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, size) == 0, "DwProcessCreateArgsV1.size offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, version) == 4, "DwProcessCreateArgsV1.version offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, task_group) == 8, "DwProcessCreateArgsV1.task_group offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, bootstrap_channel) == 16, "DwProcessCreateArgsV1.bootstrap_channel offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, process_rights) == 24, "DwProcessCreateArgsV1.process_rights offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, root_region_rights) == 32, "DwProcessCreateArgsV1.root_region_rights offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, child_bootstrap_rights) == 40, "DwProcessCreateArgsV1.child_bootstrap_rights offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, flags) == 48, "DwProcessCreateArgsV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateArgsV1, reserved) == 56, "DwProcessCreateArgsV1.reserved offset");

/* Handles returned after successful process construction. */
typedef struct DwProcessCreateResultV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Caller-local process handle. */
    DwHandle process;
    /* Caller-local root address-region handle. */
    DwHandle root_address_region;
    /* Informational child-local value; unusable by the creating process. */
    DwHandle child_bootstrap_handle;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwProcessCreateResultV1;
#define DW_PROCESS_CREATE_RESULT_V1_SIZE ((uint32_t)(64))
DW_STATIC_ASSERT(sizeof(DwProcessCreateResultV1) == 64, "DwProcessCreateResultV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwProcessCreateResultV1) == 8, "DwProcessCreateResultV1 alignment");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, size) == 0, "DwProcessCreateResultV1.size offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, version) == 4, "DwProcessCreateResultV1.version offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, process) == 8, "DwProcessCreateResultV1.process offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, root_address_region) == 16, "DwProcessCreateResultV1.root_address_region offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, child_bootstrap_handle) == 24, "DwProcessCreateResultV1.child_bootstrap_handle offset");
DW_STATIC_ASSERT(offsetof(DwProcessCreateResultV1, reserved) == 32, "DwProcessCreateResultV1.reserved offset");

/* Initial user register state for starting a created thread. */
typedef struct DwThreadStartArgsV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Created thread requiring EXECUTE. */
    DwHandle thread;
    /* Initial instruction pointer. */
    DwUserAddress entry;
    /* Initial stack pointer. */
    DwUserAddress stack_pointer;
    /* First Wyrmroot-owned initial argument value. */
    uint64_t startup_argument0;
    /* Second Wyrmroot-owned initial argument value. */
    uint64_t startup_argument1;
    /* Must be zero in ABI 0. */
    uint64_t flags;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[3];
} DwThreadStartArgsV1;
#define DW_THREAD_START_ARGS_V1_SIZE ((uint32_t)(80))
DW_STATIC_ASSERT(sizeof(DwThreadStartArgsV1) == 80, "DwThreadStartArgsV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwThreadStartArgsV1) == 8, "DwThreadStartArgsV1 alignment");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, size) == 0, "DwThreadStartArgsV1.size offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, version) == 4, "DwThreadStartArgsV1.version offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, thread) == 8, "DwThreadStartArgsV1.thread offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, entry) == 16, "DwThreadStartArgsV1.entry offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, stack_pointer) == 24, "DwThreadStartArgsV1.stack_pointer offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, startup_argument0) == 32, "DwThreadStartArgsV1.startup_argument0 offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, startup_argument1) == 40, "DwThreadStartArgsV1.startup_argument1 offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, flags) == 48, "DwThreadStartArgsV1.flags offset");
DW_STATIC_ASSERT(offsetof(DwThreadStartArgsV1, reserved) == 56, "DwThreadStartArgsV1.reserved offset");

/* One handle transaction descriptor for channel_send. */
typedef struct DwHandleTransferV1 {
    /* Source handle; duplicate source handles in one send are invalid. */
    DwHandle handle;
    /* Nonzero subset of rights held by the source handle, which must carry TRANSFER. */
    DwRights requested_rights;
    /* Must be MOVE in ABI 0. */
    DwHandleTransferOperation operation;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[2];
} DwHandleTransferV1;
#define DW_HANDLE_TRANSFER_V1_SIZE ((uint32_t)(40))
DW_STATIC_ASSERT(sizeof(DwHandleTransferV1) == 40, "DwHandleTransferV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwHandleTransferV1) == 8, "DwHandleTransferV1 alignment");
DW_STATIC_ASSERT(offsetof(DwHandleTransferV1, handle) == 0, "DwHandleTransferV1.handle offset");
DW_STATIC_ASSERT(offsetof(DwHandleTransferV1, requested_rights) == 8, "DwHandleTransferV1.requested_rights offset");
DW_STATIC_ASSERT(offsetof(DwHandleTransferV1, operation) == 16, "DwHandleTransferV1.operation offset");
DW_STATIC_ASSERT(offsetof(DwHandleTransferV1, reserved0) == 20, "DwHandleTransferV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwHandleTransferV1, reserved) == 24, "DwHandleTransferV1.reserved offset");

/* One caller-local handle created by a successful channel_receive. */
typedef struct DwReceivedHandleInfoV1 {
    /* New receiving-process handle. */
    DwHandle handle;
    /* Rights carried by the transaction. */
    DwRights rights;
    /* Received kernel object type. */
    DwObjectType object_type;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[2];
} DwReceivedHandleInfoV1;
#define DW_RECEIVED_HANDLE_INFO_V1_SIZE ((uint32_t)(40))
DW_STATIC_ASSERT(sizeof(DwReceivedHandleInfoV1) == 40, "DwReceivedHandleInfoV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwReceivedHandleInfoV1) == 8, "DwReceivedHandleInfoV1 alignment");
DW_STATIC_ASSERT(offsetof(DwReceivedHandleInfoV1, handle) == 0, "DwReceivedHandleInfoV1.handle offset");
DW_STATIC_ASSERT(offsetof(DwReceivedHandleInfoV1, rights) == 8, "DwReceivedHandleInfoV1.rights offset");
DW_STATIC_ASSERT(offsetof(DwReceivedHandleInfoV1, object_type) == 16, "DwReceivedHandleInfoV1.object_type offset");
DW_STATIC_ASSERT(offsetof(DwReceivedHandleInfoV1, reserved0) == 20, "DwReceivedHandleInfoV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwReceivedHandleInfoV1, reserved) == 24, "DwReceivedHandleInfoV1.reserved offset");

/* Actual and required capacities reported by channel_receive. */
typedef struct DwChannelReceiveResultV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Payload bytes copied on success. */
    uint32_t actual_bytes;
    /* Handle records created on success. */
    uint32_t actual_handles;
    /* Selected datagram payload size on SUCCESS or BUFFER_TOO_SMALL; unspecified on other failures. */
    uint32_t required_bytes;
    /* Selected datagram handle count on SUCCESS or BUFFER_TOO_SMALL; unspecified on other failures. */
    uint32_t required_handles;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[4];
} DwChannelReceiveResultV1;
#define DW_CHANNEL_RECEIVE_RESULT_V1_SIZE ((uint32_t)(56))
DW_STATIC_ASSERT(sizeof(DwChannelReceiveResultV1) == 56, "DwChannelReceiveResultV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwChannelReceiveResultV1) == 8, "DwChannelReceiveResultV1 alignment");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, size) == 0, "DwChannelReceiveResultV1.size offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, version) == 4, "DwChannelReceiveResultV1.version offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, actual_bytes) == 8, "DwChannelReceiveResultV1.actual_bytes offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, actual_handles) == 12, "DwChannelReceiveResultV1.actual_handles offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, required_bytes) == 16, "DwChannelReceiveResultV1.required_bytes offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, required_handles) == 20, "DwChannelReceiveResultV1.required_handles offset");
DW_STATIC_ASSERT(offsetof(DwChannelReceiveResultV1, reserved) == 24, "DwChannelReceiveResultV1.reserved offset");

/* One handle and nonzero desired signal mask for wait_many. */
typedef struct DwWaitItemV1 {
    /* Waitable handle requiring WAIT. */
    DwHandle handle;
    /* Nonzero desired signal mask. */
    DwSignals signals;
} DwWaitItemV1;
#define DW_WAIT_ITEM_V1_SIZE ((uint32_t)(16))
DW_STATIC_ASSERT(sizeof(DwWaitItemV1) == 16, "DwWaitItemV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwWaitItemV1) == 8, "DwWaitItemV1 alignment");
DW_STATIC_ASSERT(offsetof(DwWaitItemV1, handle) == 0, "DwWaitItemV1.handle offset");
DW_STATIC_ASSERT(offsetof(DwWaitItemV1, signals) == 8, "DwWaitItemV1.signals offset");

/* Deterministic wait result; the lowest ready input index wins. */
typedef struct DwWaitResultV1 {
    /* Byte size of this structure. */
    uint32_t size;
    /* Structure version; set to one. */
    uint32_t version;
    /* Lowest ready input index, or zero for wait_one. */
    uint32_t index;
    /* Reserved; must be zero. */
    uint32_t reserved0;
    /* Full observed signal state for the selected item. */
    DwSignals observed;
    /* Reserved; all elements must be zero. */
    uint64_t reserved[3];
} DwWaitResultV1;
#define DW_WAIT_RESULT_V1_SIZE ((uint32_t)(48))
DW_STATIC_ASSERT(sizeof(DwWaitResultV1) == 48, "DwWaitResultV1 size");
DW_STATIC_ASSERT(DW_ALIGNOF(DwWaitResultV1) == 8, "DwWaitResultV1 alignment");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, size) == 0, "DwWaitResultV1.size offset");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, version) == 4, "DwWaitResultV1.version offset");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, index) == 8, "DwWaitResultV1.index offset");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, reserved0) == 12, "DwWaitResultV1.reserved0 offset");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, observed) == 16, "DwWaitResultV1.observed offset");
DW_STATIC_ASSERT(offsetof(DwWaitResultV1, reserved) == 24, "DwWaitResultV1.reserved offset");

#undef DW_ALIGNOF
#undef DW_STATIC_ASSERT

#endif /* DEEPWYRM_ABI_GENERATED_H */
