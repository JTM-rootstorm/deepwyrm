# ABI schema placeholder

This directory is intentionally empty of schema definitions. Future DW0-A work
will add the canonical, reviewed schema here before any generated consumer
definitions are introduced.

Do not infer ABI values, layouts, or semantics from this placeholder.
