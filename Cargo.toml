[workspace]
members = [
    "crates/deepwyrm-abi",
    "kernel",
    "tools/abi-gen",
    "tools/xtask",
]
resolver = "2"

[workspace.package]
license = "GPL-2.0-or-later"

[profile.dev]
debug = 2
panic = "abort"

[profile.release]
debug = 2
panic = "abort"
