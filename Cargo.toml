[workspace]
members = [
    "crates/deepwyrm-abi",
    "kernel",
    "tools/abi-gen",
    "tools/xtask",
]
resolver = "2"

[profile.dev]
debug = 2
panic = "abort"

[profile.release]
debug = 2
panic = "abort"
